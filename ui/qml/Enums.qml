import QtQuick 2.15

QtObject {

    enum WindowStatus{
        Normal,
        Maximized,
        Minimized
    }

    enum SnapPosition{
        None,
        Top,
        TopLeft,
        TopRight,
        Left,
        Right,
        BottomLeft,
        BottomRight
    }

}
